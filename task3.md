# 完成订单提交页面

## 1. 修改现有订单确认页面

根据 `swagger-order.json` 中的接口信息， 修改 `ConfirmOrder.vue` 。
现在每次确认订单接口会多返回一个token，用于订单防重验证。

## 2. 完成订单提交功能

根据接口信息完成订单提交功能。

## 3. 跳转支付页面

若订单创建成功（R.code == 0），则跳转到接口信息中的支付接口，该接口会直接返回支付页面，否则显示错误信息。