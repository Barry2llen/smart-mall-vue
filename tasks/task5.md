# 完善商城首页页面

## 1. 登录等待动画

在等待完成OAuth登录时，要有明确的加载/等待动画。当前只有一行小字，不够醒目。

## 2. 三级分类展示

根据 `swagger-product.json` 中的 `商品接口` -> `以树形结构查出所有分类以及子分类` 接口，展示三级分类。
当鼠标移动到某个分类时，该分类的子分类会以弹出层的形式展示出来。点击某个分类时，可以跳转到该分类的商品的搜索页面。

## 3. 新增秒杀场次及商品展示

根据 `swagger-flash-sale.json` 中的 `秒杀接口` -> `获取秒杀活动场次列表` 接口，展示秒杀商品。根据api文档传入对应参数。